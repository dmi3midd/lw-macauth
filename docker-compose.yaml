version: '3.8'

services:
  lw-macauth:
    build: .
    container_name: lw-macauth
    restart: unless-stopped
    ports:
      - "2700:2700"
    environment:
      CONFIG_PATH: /app/config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./storage:/app/storage
